#include <stdio.h>

void print_alphabet() {
	char letter;
		for (letter = 'a'; letter <= 'z'; letter++) {
		 printf("%c\n", letter);				    }
}
	int main() {
		print_alphabet();
	        return 0;
}

